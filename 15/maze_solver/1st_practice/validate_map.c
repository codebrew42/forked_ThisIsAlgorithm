#include "inc/maze_solver.h"

/* how to validate recursively? [backtracking]

steps
[1] move_func()
		cur_dir = 0;
		move[cur_dir]
		if (GOAL)
			return ()
		if (WALL)
			move_func(cur_dir++)
		else
			move_fuc(cur_dir)
[2]


base case : if(GOAL found) then {return 0}

*/
int	validate_map(t_map *m)
{
	m

}